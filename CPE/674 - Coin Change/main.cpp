#include <bits/stdc++.h>
#define ll long long
#define eb emplace_back
#define endl '\n'
using namespace std;

int main() {
    // 50, 25, 10, 5, 1-cent
    ios::sync_with_stdio(0);
    cin.tie(NULL);
    int n;
    while (cin >> n) {
        if (n <= 0) {
            cout << 0 << endl;
        }
        else {
            
        }
    }
}